[project]
name = ""
description = ""
readme = "README.md"
authors = [
    { name = "Lidocondido", email = "55406615+Lidocondido@users.noreply.github.com" }
]
requires-python = ">=3.14"

dynamic = ["version"]

#dependencies = [
#    "textual>=6.7.0",
#    "wmi>=1.5.0",
#]

#[tool.setuptools_scm]
#write_to = "src/vesta/_version.py"

#[[tool.setuptools-rust.ext-modules]]
#target = "vesta.vesta_rust"
#path = "rust/Cargo.toml"
#binding = "PyO3"

#[tool.setuptools]
#package-dir = { "" = "src" }

#[tool.setuptools.packages.find]
#where = ["src"]

#dependencies = []

#[project.scripts]
#vesta = "vesta:main"

#[build-system]
#requires = ["setuptools>=80", "setuptools_scm>=9","setuptools-rust>=1.12"]
#build-backend = "setuptools.build_meta"


#[dependency-groups]
#dev = [
#    "pre-commit>=4.5.0",
#    "pytest>=9.0.1",
#    "setuptools>=80.9.0",
#    "setuptools-rust>=1.12.0",
#    "setuptools-scm>=9.2.2",
#]


# file generated by setuptools-scm
# don't change, don't track in version control

__all__ = [
    "__version__",
    "__version_tuple__",
    "version",
    "version_tuple",
    "__commit_id__",
    "commit_id",
]

# setuotols_scm automatically writes this file.
# this is _version.py file generated by setuptools-scm

TYPE_CHECKING = False
if TYPE_CHECKING:
    from typing import Tuple
    from typing import Union

    VERSION_TUPLE = Tuple[Union[int, str], ...]
    COMMIT_ID = Union[str, None]
else:
    VERSION_TUPLE = object
    COMMIT_ID = object

version: str
__version__: str
__version_tuple__: VERSION_TUPLE
version_tuple: VERSION_TUPLE
commit_id: COMMIT_ID
__commit_id__: COMMIT_ID

__version__ = version = '0.0.2.dev1+ge7942a0ab.d20251201'
__version_tuple__ = version_tuple = (0, 0, 2, 'dev1', 'ge7942a0ab.d20251201')

__commit_id__ = commit_id = 'ge7942a0ab'

